# gits

Unix 风格的 GitHub 内容获取与同步 CLI。

## 安装（pipx）

```bash
pipx install .
```

安装后可直接使用 `gits`：

```bash
gits help
gits https://github.com/user/repo/tree/main/src ./local-src
gits push "feat: add new scripts"
```
